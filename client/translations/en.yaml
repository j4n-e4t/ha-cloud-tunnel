configuration:
  server_addr:
    name: Server Address
    description: "Tunnel server address with port (e.g., your-app.railway.app:7777)"
  token:
    name: Token
    description: "Setup token from the server (sk-...)"
  fingerprint:
    name: Fingerprint
    description: "Server certificate fingerprint (SHA256:...) for secure connection"
